<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tigerobo.x.pai.dal.ai.mapper.SpatioActionQueryMapper">

    <select id="query" parameterType="map" resultType="java.lang.Integer">
        select ai.id from ai_spatio_action ai left join `xpai-auth-user` u on ai.user_id = u.id
        where ai.status=1 and ai.is_deleted = 0
        and
        (ai.title like concat('%',#{req.keyword},'%') or u.name like concat(#{req.keyword},'%'))
        order by ai.publish_time desc
    </select>
</mapper>